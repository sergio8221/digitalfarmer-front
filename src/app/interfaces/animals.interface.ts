
/**
 * Animal interface for in-app use
 */
export interface Animal {
    id: number,
    code: string,
    name: string,
    species: string,
    born: Date,
    sex: string,
    placing: string,
    health: string
}